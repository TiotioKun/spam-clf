# Зависимости только для веб-сервиса.
# Ноутбучные библиотеки (jupyter, matplotlib, seaborn, wordcloud) сюда
# не входят намеренно: на хостинге они не нужны, а сборку замедляют.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
jinja2>=3.1
joblib>=1.3
numpy>=1.24
pandas>=2.0

# ВАЖНО: версия должна совпадать с той, где обучалась модель,
# иначе joblib.load выдаст предупреждения или упадёт.
# Точное значение — в models/model_meta.json, поле sklearn_version.
scikit-learn==1.6.1

lightgbm>=4.0
